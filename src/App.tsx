/**
 * Monark v2 — placeholder de fondations.
 * Aucune logique, aucun composant. Confirme juste que le terrain est prêt.
 */
export default function App() {
  return <h1>Monark v2 — fondations OK</h1>;
}